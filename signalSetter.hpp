#ifndef SIGNAL_SETTER_HPP
#define SIGNAL_SETTER_HPP

class signalSetter
{
private:
    /* data */
public:
    signalSetter(/* args */);
    ~signalSetter();
};

signalSetter::signalSetter(/* args */)
{
}

signalSetter::~signalSetter()
{
}


#endif